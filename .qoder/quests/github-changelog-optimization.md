# GitHub Changelog 展示优化设计

## 设计目标

优化 `packages/changelog-github` 生成的 changelog 在 GitHub 上的展示效果，在保留核心信息的前提下，使其更加简洁、美观、易读。

## 当前问题分析

### 现有格式特征

当前生成的 changelog 条目采用单行内联模式，所有元数据通过 `·` 分隔符连接在一起：

```
- ✨ **Add new lint rule** — ensure coverage for edge cases · [#123](link) · [`abcdef1`](link) · Thanks [@user](link) · Minor release
```

存在的体验问题：

1. **信息密度过高**：标题、详情、PR链接、commit、作者、发布类型全部挤在一行，视觉负担重
2. **次要信息干扰阅读**：commit SHA、release type 等辅助信息占据显著位置，削弱了核心变更内容的可读性
3. **缺乏层次感**：所有信息平铺，无法快速定位关键内容
4. **移动端体验差**：长行在小屏幕设备上需要横向滚动
5. **重复表达**：emoji 已经暗示了 release type，文字 "Minor release" 显得冗余

## 优化策略

### 信息优先级划分

| 优先级  | 信息类型          | 展示策略           |
| ------- | ----------------- | ------------------ |
| P0 核心 | 变更标题          | 醒目展示，保持粗体 |
| P0 核心 | PR 链接           | 紧跟标题，便于追溯 |
| P1 重要 | 详细说明          | 换行展示，保持清晰 |
| P2 辅助 | 贡献者            | 简化为小字提示     |
| P3 次要 | Commit SHA        | 移除或折叠         |
| P3 次要 | Release Type 文字 | 移除（保留 emoji） |

### 布局结构调整

采用紧凑型多行布局，将信息分层展示：

```
主行：emoji + 标题 + PR链接 + 贡献者标签
详情行：详细说明（如果存在）
```

核心原则：

- 主行长度控制在 80 字符以内
- 使用轻量级标记替代冗长文字
- 优化 GitHub Markdown 渲染效果

## 格式设计方案

### 主行构成规则

**基础格式**：

```
- {emoji} **{标题}** [{PR}] {作者标签}
```

**组件说明**：

| 组件     | 格式            | 示例                | 说明                       |
| -------- | --------------- | ------------------- | -------------------------- |
| emoji    | 单个表情符号    | `✨`                | 保留现有 release type 映射 |
| 标题     | `**粗体文本**`  | `**Add lint rule**` | 核心变更描述               |
| PR 链接  | `[#数字](链接)` | `[#123](url)`       | 紧贴标题，无额外分隔符     |
| 作者标签 | `by @用户名`    | `by @octocat`       | 简化的贡献者提示           |

**渲染示例**：

```
- ✨ **Add new lint rule** [#123] by @octocat
```

### 详情行处理

当 changeset 包含补充说明时，在主行下方展示为缩进列表：

```
- ✨ **Add new lint rule** [#123] by @octocat
  - ensure coverage for edge cases
  - add TypeScript type guards
```

缩进深度：2 个空格（符合 Markdown 标准）

### 特殊场景处理

#### 场景一：无 PR 信息

当没有 PR 链接时，保留 commit 链接（采用短 SHA）：

```
- 🐛 **Fix parsing error** [`a1b2c3d`] by @user
```

#### 场景二：无作者信息

当无法识别贡献者时，省略作者标签：

```
- 📝 **Update documentation** [#456]
```

#### 场景三：无详情说明

主行即为完整条目：

```
- 🚀 **Release v2.0** [#789] by @maintainer
```

#### 场景四：依赖更新

依赖更新条目保持单行，优化包名展示：

```
- 📦 **Dependencies** [`abc123`]
  → `eslint@^9.0.0`, `vitest@^2.0.0`
```

或简化为：

```
- 📦 Updated 3 dependencies [`abc123`]
```

## 实现调整点

### 数据结构改动

无需调整现有数据结构，仅修改渲染逻辑。

### 渲染逻辑变更

需要修改的核心函数：`getReleaseLine`

**调整要点**：

1. **元数据段简化**
   - 移除 "Thanks" 前缀，改为 "by"
   - 移除 "Minor release" 等文字标签
   - 移除 commit SHA（仅在无 PR 时保留）

2. **分隔符优化**
   - 主行组件间使用空格直接连接
   - 移除所有 `·` 分隔符
   - PR/commit 链接用方括号包裹

3. **详情行处理**
   - 详情内容从主行移到缩进子列表
   - 每个详情点独立成行
   - 保持 2 空格缩进

4. **作者信息处理**
   - 优先使用 changeset 中声明的作者
   - 回退到 GitHub API 返回的用户
   - 格式统一为 `by @username`（不带超链接，利用 GitHub 自动识别）

### 依赖更新逻辑变更

需要修改的核心函数：`getDependencyReleaseLine`

**调整要点**：

1. **包列表简化**
   - 当依赖数量 ≤ 3 时，展示完整列表
   - 当依赖数量 > 3 时，显示数量摘要

2. **格式调整**

   ```
   // ≤ 3 个依赖
   - 📦 **Dependencies** [`commit`]
     → `pkg1@1.0.0`, `pkg2@2.0.0`

   // > 3 个依赖
   - 📦 Updated 5 dependencies [`commit`]
   ```

3. **Commit 引用处理**
   - 多个 commit 时取第一个
   - 采用短 SHA 格式

## 效果对比

### 优化前

```
- ✨ **Add optional `tailwind-config` flag** — accepts a relative path to `tailwind.config.ts` · falls back to workspace root when unset · [#42](link) · [`1234567`](link) · Thanks [@octocat](link) · Minor release
```

字符数：约 210 字符（单行）

### 优化后

```
- ✨ **Add optional `tailwind-config` flag** [#42] by @octocat
  - accepts a relative path to `tailwind.config.ts`
  - falls back to workspace root when unset
```

字符数：约 160 字符（3 行）

### 改进效果

| 维度       | 改进幅度                   |
| ---------- | -------------------------- |
| 主行长度   | 减少 60%                   |
| 视觉噪音   | 减少 5 个分隔符            |
| 可读性     | 信息分层，扫描效率提升     |
| 移动端体验 | 无需横向滚动               |
| 语义清晰度 | 核心信息突出，次要信息收敛 |

## 兼容性考量

### 向后兼容

格式变更不影响：

- Changesets 配置接口
- 函数签名
- 输入参数结构

现有 changeset 文件无需修改即可生成新格式。

### GitHub 渲染

优化后的格式完全符合 GitHub Flavored Markdown 规范：

- `@username` 自动转换为用户链接
- `#数字` 在仓库上下文中自动转换为 PR 链接（需验证 Changesets 生成的 changelog 上下文）
- Emoji 正常显示
- 缩进列表正确解析

### 测试兼容

现有单元测试需要更新断言内容，但测试逻辑和覆盖范围保持不变。

## 实施优先级

### 第一阶段：核心格式优化

1. 简化主行元数据（移除 "Thanks"、"release" 文字）
2. 优化分隔符（用空格和方括号替代 `·`）
3. 作者信息改为 `by @user` 格式

预期收益：立即减少 30% 的视觉噪音

### 第二阶段：详情行分离

1. 将 detailLines 从主行移到缩进子列表
2. 调整换行和缩进逻辑

预期收益：主行长度减少 50%

### 第三阶段：智能信息折叠

1. 移除无 PR 时的 commit SHA
2. 依赖更新采用数量摘要

预期收益：极端场景下的展示更优雅

## 测试验证策略

### 单元测试更新

需要更新的测试文件：`test/index.test.ts`

**更新范围**：

- 快照断言中的格式字符串
- 正则匹配规则调整为新格式
- 新增边界情况测试（无 PR、无作者、多依赖等）

### 视觉验证方法

1. **本地预览**
   - 在测试仓库生成实际 changelog
   - 在 GitHub 上查看渲染效果（创建 draft release）

2. **多场景验证**
   - major/minor/patch 各类型发布
   - 有/无 PR 的情况
   - 单/多贡献者
   - 简单/复杂的详情说明
   - 依赖更新（1个/5个/10个）

3. **设备测试**
   - 桌面端浏览器
   - 移动端浏览器
   - GitHub mobile app

### 回滚预案

如果新格式出现兼容性问题，可以通过以下方式快速回滚：

1. 恢复 `src/index.ts` 中的渲染逻辑
2. 运行 `pnpm build` 重新构建
3. 发布 patch 版本

## 文档同步

需要更新的文档文件：`README.md` 和 `README.zh.md`

**更新内容**：

1. 示例输出调整为新格式
2. 更新特性说明中关于元数据展示的描述
3. 补充格式优化的设计理念说明

**示例章节调整**：

将 "Writing Changeset Summaries" 章节的渲染示例从：

```
- Add optional `tailwind-config` flag to the lint preset (sonofmagic/dev-configs#42 · `1234567` · Thanks [@octocat](link)!)
  - accepts a relative path to `tailwind.config.ts`
  - falls back to the workspace root when unset
> MINOR release
```

更新为：

```
- ✨ **Add optional `tailwind-config` flag** [#42] by @octocat
  - accepts a relative path to `tailwind.config.ts`
  - falls back to the workspace root when unset
```

## 发布计划

### 版本策略

此次优化涉及输出格式变更，虽不影响 API，但属于用户可见的行为变化，建议发布为：

**Minor 版本升级**（如 `0.0.2` → `0.1.0`）

理由：

- 功能增强（更优的展示效果）
- 不破坏现有集成
- 用户需感知格式变化

### Changeset 内容

建议的 changeset 摘要：

```
Optimize changelog formatting for better GitHub readability

- Simplify metadata presentation (remove redundant separators and labels)
- Move detailed descriptions to indented sub-lists
- Shorten contributor mentions to "by @username" format
- Reduce visual noise while preserving essential information
```

### 发布检查清单

- [ ] 所有单元测试通过
- [ ] 在真实仓库验证渲染效果
- [ ] 更新 README 文档
- [ ] 更新 README.zh 文档
- [ ] 创建 changeset
- [ ] 审查生成的 CHANGELOG 预览
- [ ] 发布到 npm
- [ ] 在下游项目验证新版本
